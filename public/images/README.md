# Logo Placement

Place your logo file here as `logo.png` for it to appear in the header.

**Recommended specifications:**
- Format: PNG with transparent background
- Size: 200x80 pixels (or similar aspect ratio)
- File name: `logo.png`

The header will automatically use your logo when available, with fallback to the sun/moon icons if no logo is found.